services:
  api:
    build: .
    image: skin-disease:latest
    container_name: skin-disease-api
    volumes:
      - ./models:/app/models:z
    env_file:
      - .env
    networks:
      - private-network
  nginx:
    build: ./frontend
    image: skin-disease-nginx:latest
    container_name: skin-disease-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro,z
    networks:
      - public-network
      - private-network
    depends_on:
      - api


networks:
  public-network:
    driver: bridge
    internal: false
  private-network:
    driver: bridge
    internal: true